@model SchoolManagement.Models.ViewModel.ExamEditViewModel

@{
    ViewData["Title"] = "Create";
}

<h1>@(Model != null && Model.ExamId > 0 ? "Update" : "Create")</h1>

<h4>Exam</h4>
<hr />
@if (TempData["Message"] != null)
{
    <script>
        alert('@TempData["Message"]');
    </script>
}
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            @if (Model.ExamId > 0)
            {
                <input type="hidden" asp-for="ExamId" />
            }
            <div class="form-group">
                <label asp-for="ExamTypeId" class="control-label"></label>
                <select asp-for="ExamTypeId" class="form-control" asp-items="ViewBag.ExamTypeId">
                    <option value="">Select Grade</option>
                </select>
                <span asp-validation-for="ExamTypeId" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="StartDate" class="control-label"></label>
                <input asp-for="StartDate" class="form-control" />
                <span asp-validation-for="StartDate" class="text-danger"></span>
            </div>

            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>
